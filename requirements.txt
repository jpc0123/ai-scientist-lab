# Scientist Lab — Python environment (host / workbench)
# Python >= 3.11
# Install: pip install -r requirements.txt
# Optional editable install: pip install -e ".[dev]"
# Optional host COCO eval: pip install -e ".[eval]"

# API / orchestration
docker>=7.0.0
pydantic>=2.0.0
pydantic-settings>=2.0.0
sqlalchemy>=2.0.0
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
httpx>=0.27.0
PyYAML>=6.0.0
jsonschema>=4.20.0
referencing>=0.35.0

# Dataset preview / slice helpers (host)
Pillow>=10.0.0
numpy>=1.26.0

# Dev / tests
pytest>=8.0.0

# Optional: host-side APS_lowlight (COCO AP_small). Uncomment if you evaluate
# on the host; GPU training image uses faster-coco-eval instead.
# pycocotools>=2.0.7

# Notes:
# - GPU training runs inside Docker image scientist-rgbt-detection:v2-cuda
#   (see dockerfiles/rgbt-detection-v2-cuda/requirements.txt for in-container deps:
#    numpy, Pillow, PyYAML, faster-coco-eval, scipy, loguru, tensorboard,
#    calflops, transformers, matplotlib; torch comes from the CUDA base image).
# - Remote GPU worker: worker/requirements.txt
# - Frontend (React/Vite): web/package.json  — Node.js 18+, npm install
# - LLM Planner/Reviewer need an OpenAI-compatible API endpoint + API key via env.
