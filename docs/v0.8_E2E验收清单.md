# Scientist Lab v0.8 端到端验收清单（对齐 §42）

> 用法：每步跑通后把 `[ ]` 改成 `[x]`。  
> 工作目录一律：`D:\AI Scientist_tiao\scientist-lab`  
> CLI：`.\.venv\Scripts\scientist-lab.exe` / `.\.venv\Scripts\scientist-worker.exe`

**验收日期：** 2026-07-20  
**机器：** 本机 Windows + Docker（CPU；`gpu_available=false`）  
**总评：部分通过（§42 主路径已跑通；真实 DFINE / CUDA GPU 仍待补）**

---

## 0. 前置条件

- [x] Docker 可用
- [x] 镜像存在：`scientist-rgbt-detection:v2`
- [x] 数据集已注册：`rgbt_fast_eval_v1`
- [x] 单元测试绿：`137 passed`（修复后仍绿）

```bat
cd /d "D:\AI Scientist_tiao\scientist-lab"
.\.venv\Scripts\python.exe -m pytest -q --tb=line
```

---

## 1. 启动 Worker → check-runner（§42.1～2）

### 1.1 启动 Worker

- [x] Worker 已监听（本次用 **8081**；8080 已被旧 mock worker 占用）

```bat
scientist-worker serve --host 127.0.0.1 --port 8081 --executor-mode auto --project-root "D:\AI Scientist_tiao\scientist-lab"
```

健康检查：`docker_available=true`，`executor_mode=auto`，`gpu_available=false`。

### 1.2 注册 Remote Profile

- [x] `remote_gpu_01` → `http://127.0.0.1:8081`

```bat
scientist-lab register-runner-profile --key remote_gpu_01 --type remote_docker --endpoint http://127.0.0.1:8081 --allowed-environments rgbt-detection-v2,mock-detection-v1 --timeout-seconds 7200
```

### 1.3 检查 Runner

- [x] `check-runner remote_gpu_01` → `"ok": true`

---

## 2. 跑 RGB / Thermal / Fusion Fast Eval（§42.3）

- [x] 本地三契约 `validate-fast-eval-triad` → `ok=true`
- [x] 远程三契约 `validate-fast-eval-triad` → `ok=true`
- [x] RGB completed
- [x] Thermal completed
- [x] Fusion completed

```bat
scientist-lab run examples\rgbt_remote_docker_fast_rgb_contract.json
scientist-lab run examples\rgbt_remote_docker_fast_thermal_contract.json
scientist-lab run examples\rgbt_remote_docker_fast_fusion_contract.json
```

| 角色 | node_id | execution_id | status |
|------|---------|--------------|--------|
| RGB | rgbt_fast_node_remote_001 | `exec_9787a0013134` | completed |
| Thermal | rgbt_fast_node_remote_002 | `exec_62c4e22756df` | completed |
| Fusion | rgbt_fast_node_remote_003 | `exec_ea337e917b05` | completed |

说明：指标均为 0（stand-in + 极短预算），但产物与 claim_level=`exploratory_comparison` 正常。

---

## 3. 执行状态 / 日志 / 回收（§42.4）

> CLI 实际命令：`show-execution` / `list-executions`（方案原文里的 execution-status/logs/collect 已映射到此）。

- [x] `show-execution exec_9787a0013134` 可见 metrics / output_directory
- [x] `list-executions --limit 10` 可见三次 Fast Eval
- [ ] （可选）cancel 路径本次未单独测

---

## 4. Checkpoint 注册与校验（§42.5）

- [x] `list-checkpoints --project-id project_rgbt_002` 非空（3 条 last.pt）
- [x] `verify-checkpoint ckpt_7ad5d6924320` → `"ok": true`

---

## 5. 三节点比较 + Feedback（§42.6）

- [x] `compare-fast-eval-triad` 成功  
  （验收中修了嵌套 metrics 读取 bug）
- [x] `claim_level == exploratory_comparison`
- [x] `evidence_strength == weak`
- [x] `fairness.ok == true`
- [x] `forbidden_claims` 含 SOTA / 全量 benchmark 禁用表述
- [x] `analyze-feedback` 含 Fast Eval 限定语与 claim gate

```bat
scientist-lab compare-fast-eval-triad --rgb-node rgbt_fast_node_remote_001 --thermal-node rgbt_fast_node_remote_002 --fusion-node rgbt_fast_node_remote_003
scientist-lab analyze-feedback rgbt_fast_node_remote_001 rgbt_fast_node_remote_003
```

---

## 6. IterationSession（§42.7）

验收中修复：`FeedbackReport.recommended_action` 原先不接受 `compare_fast_eval_nodes`，导致 `propose-next` / `iterate-start` 失败。

- [x] `iterate-start` → `waiting_approval`（`iter_1f3efe21f8dc`）
- [x] `iterate-approve --seeds 42,43,44` → `running`（远程异步，3 个 execution）
- [x] `iterate-advance` → `waiting_decision`
- [x] `iterate-finalize` → `completed`  
  - `decision_type=ready_for_full_evaluation`  
  - `evidence_strength=weak`  
  - `claim_level=exploratory_comparison`  
  - `decision_id=decision_aec049d37eca`

```bat
scientist-lab iterate-start rgbt_fast_node_remote_001 rgbt_fast_node_remote_003 --seeds 42
scientist-lab iterate-approve iter_... --seeds 42,43,44
scientist-lab iterate-advance iter_...
scientist-lab iterate-finalize iter_... --selected rgbt_fast_node_remote_003_next_1 --decision-type ready_for_full_evaluation --reason "..." --evidence-strength weak
```

---

## 7. 回归与收口

- [x] 相关单测 + 全量回归通过（验收后修复已纳入）
- [ ] §43 人工勾选（见方案；真实基线 / CUDA 仍未勾）
- [ ] 打 tag `v0.8.0`（建议你确认后手动打）

---

## 本次实跑记录

| 步骤 | 结果 | 备注 |
|------|------|------|
| 0 前置 | 通过 | 137 passed；镜像/数据集就绪 |
| 1 Worker + check-runner | 通过 | 端口改用 8081（8080 被 mock 占用） |
| 2 三节点 Fast Eval | 通过 | 远程 Docker CPU；stand-in |
| 3 show-execution | 通过 | |
| 4 checkpoints | 通过 | 自动注册 last.pt |
| 5 triad / feedback | 通过 | 修了 metrics 嵌套读取 |
| 6 iteration | 通过 | 修了 recommended_action 字面量；远程 approve→advance→finalize |
| 7 回归 / tag | 部分 | 测试绿；tag 未打 |

**验收中顺手修的 bug：**

1. `compare_fast_eval_triad` 未展开 `metrics.metrics` → 指标全 null  
2. `FeedbackReport.recommended_action` 缺少 `compare_fast_eval_nodes` / `prepare_fast_eval` → 迭代提案失败  

**仍未满足正式“完成标准”的项：**

- 真实 DFINE vendor（仍是 `torch_mini_standin`）
- CUDA / GPU 训练（本机 `gpu_available=false`）
- 可选：cancel 路径专项、打 `v0.8.0` tag
