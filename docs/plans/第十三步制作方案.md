# 第十三步制作方案：v1.3 真实 LLM Provider、Replay 与质量评测

> **本文档为 v1.3 权威实现方案。**  
> 承接 `第十二步制作方案.md`（v1.2）。  
> Tag 基线：`v1.2.0` @ `78d2c1b`；分支：`feat/v1.3-llm-provider`。  
> **第一阶段不做：** 真实云模型、改 Planner/Critic 业务逻辑。

---

## 0. 第一阶段范围（当前）

```text
LLMProvider 统一接口
→ FakeProvider
→ ReplayProvider
→ 请求/响应审计
→ JSON Schema 校验
```

**第一个可验收目标：**

```text
同一 PlanningContext
→ FakeProvider 返回固定结构化结果
→ 保存调用记录
→ ReplayProvider 读取历史响应
→ 输出与原响应一致
→ 不访问网络
```

完成后，再把 v1.0 Planner / Critic 接到 `LLMProvider`（v1.3.6+）。

---

## 1. 包结构（第一阶段）

```text
src/scientist_lab/llm/
├── __init__.py
├── models.py
├── provider.py
├── fake_provider.py
├── replay_provider.py
├── repository.py
├── schema_parser.py
└── audit.py
```

---

## 2. 分阶段

| 子版本 | 内容 | 状态 |
|--------|------|------|
| **v1.3.1** | 统一 `LLMProvider` 接口 | **已完成** |
| **v1.3.2** | `FakeProvider` | **已完成** |
| **v1.3.3** | `ReplayProvider` | **已完成** |
| **v1.3.4** | 请求与响应审计 | **已完成** |
| **v1.3.5** | JSON Schema 校验 | **已完成** |
| v1.3.6 | Planner Provider 接入 | 待做 |
| v1.3.7 | Critic Provider 接入 | 待做 |
| v1.3.8 | Mock / Replay / Real 质量评测 | 待做 |
| v1.3.9 | Token、费用、延迟限制 | 待做 |
| v1.3.10 | 有限实验树端到端验收 | 待做 |

---

## 3. 接口草图

```python
class LLMProvider(Protocol):
    def complete(self, request: LLMRequest) -> LLMResponse: ...

class LLMRequest(BaseModel):
    purpose: Literal["planner", "critic", "other"]
    messages: list[dict]
    response_schema: dict | None = None
    temperature: float = 0.0
    max_tokens: int | None = None
```

Replay 键：`request_fingerprint = sha256(purpose + messages + schema)`。

---

## 4. 完成标准（第一阶段）

- [x] FakeProvider 对固定 PlanningContext 返回可 Schema 校验的 JSON
- [x] 每次调用写入审计记录（request / response / usage / latency）
- [x] ReplayProvider 按 fingerprint 回放，与原响应一致
- [x] 全程无网络访问
- [x] 不修改 Planner / Critic 业务逻辑
- [x] 相关单测通过；全量回归不破坏

真实云调用延期到 Provider 接入 Planner/Critic 之后。
