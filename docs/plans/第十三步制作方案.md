# 第十三步制作方案：v1.3 真实 LLM Provider、Replay 与质量评测

> **本文档为 v1.3 权威实现方案。**  
> 承接 `第十二步制作方案.md`（v1.2）。  
> Tag 基线：`v1.2.0` @ `78d2c1b`；封版标签：`v1.3.0`。  
> 分支：`feat/v1.3-llm-provider`（封版后仅接受修复）。  
> **本版本不做：** 真实云模型。默认路径仍为 MockPlanner / MockCritic。  
> **下一版本：** 见 `第十四步制作方案.md`（v1.4 OpenAI-compatible Provider）。

---

## 0. 范围

### 第一阶段（v1.3.1–1.3.5，已完成）

```text
LLMProvider 统一接口
→ FakeProvider
→ ReplayProvider
→ 请求/响应审计
→ JSON Schema 校验
```

### 第二阶段（v1.3.6–1.3.7，已完成）

```text
ProviderPlanner / ProviderCritic 适配器
→ build_planner_critic(mode=mock|fake|replay)
→ AgentPlanningService.configure_provider
→ plan-next / review-plan --provider
→ 默认仍为 mock；fake/replay 走 AuditingProvider，无网络
```

### 第三阶段（v1.3.8–1.3.9，已完成）

```text
Mock / Fake / Replay 质量评测报告（Real 延期为 skipped）
→ Token / 费用 / 延迟限额（LimitingProvider）
→ CLI: llm-eval / llm-usage
```

---

## 1. 包结构

```text
src/scientist_lab/llm/
├── __init__.py
├── models.py
├── provider.py
├── fake_provider.py
├── replay_provider.py
├── repository.py
├── schema_parser.py
├── audit.py
├── context_codec.py
├── quality.py          # v1.3.8
└── limits.py           # v1.3.9

src/scientist_lab/agents/
└── provider_bridge.py
```

---

## 2. 分阶段

| 子版本 | 内容 | 状态 |
|--------|------|------|
| **v1.3.1** | 统一 `LLMProvider` 接口 | **已完成** |
| **v1.3.2** | `FakeProvider` | **已完成** |
| **v1.3.3** | `ReplayProvider` | **已完成** |
| **v1.3.4** | 请求与响应审计 | **已完成** |
| **v1.3.5** | JSON Schema 校验 | **已完成** |
| **v1.3.6** | Planner Provider 接入 | **已完成** |
| **v1.3.7** | Critic Provider 接入 | **已完成** |
| **v1.3.8** | Mock / Replay / Real 质量评测 | **已完成**（Real=deferred） |
| **v1.3.9** | Token、费用、延迟限制 | **已完成** |
| **v1.3.10** | 有限实验树端到端验收 | **已完成**（离线） |

---

## 3. 接口草图

```python
class LLMProvider(Protocol):
    def complete(self, request: LLMRequest) -> LLMResponse: ...

class LLMRequest(BaseModel):
    purpose: Literal["planner", "critic", "other"]
    messages: list[dict]
    response_schema: dict | None = None
    temperature: float = 0.0
    max_tokens: int | None = None
```

Replay 键：`request_fingerprint = sha256(purpose + messages + schema)`。

---

## 4. 完成标准（第一阶段）

- [x] FakeProvider 对固定 PlanningContext 返回可 Schema 校验的 JSON
- [x] 每次调用写入审计记录（request / response / usage / latency）
- [x] ReplayProvider 按 fingerprint 回放，与原响应一致
- [x] 全程无网络访问
- [x] 不修改 Planner / Critic 业务逻辑
- [x] 相关单测通过；全量回归不破坏

真实云调用仍延期；下一阶段为 v1.3.8 质量评测。

---

## 5. 完成标准（第二阶段：Provider 接入）

- [x] `ProviderPlanner` / `ProviderCritic` 经 `LLMProvider` 产出结构化结果
- [x] `build_planner_critic("mock"|"fake"|"replay")`；默认 mock
- [x] CLI `plan-next --provider` / `review-plan --provider`
- [x] Fake→审计→Replay 路径可在服务层复现；`context_codec` 使用稳定 fingerprint 载荷
- [x] 无真实云调用；全量回归通过（267 passed）

---

## 6. 完成标准（第三阶段：质量评测 + 限额）

- [x] `evaluate_provider_quality` 产出可对比 Mock / Fake / Replay 报告；Real 标记 deferred
- [x] Fake 确定性 + Fake↔Replay 内容一致门禁
- [x] `LimitingProvider` 强制 max_calls / tokens / cost / latency
- [x] CLI `llm-eval` / `llm-usage`
- [x] 合成离线费率计量（非真实云账单）
- [x] 相关单测通过；全量回归 **268 passed**

---

## 7. 完成标准（第四阶段：树端到端离线验收）

- [x] `tree-plan-next --provider mock|fake|replay`
- [x] `scripts/accept_v13.py` → **9/9 ok**（无云调用）
- [x] 验收报告：`docs/acceptance/v1.3/`
