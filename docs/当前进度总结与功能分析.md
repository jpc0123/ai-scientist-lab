# Scientist Lab 当前进度总结与功能分析

> **核验日期：** 2026-07-25  
> **上一正式 tag：** `v2.1.0` @ `bb47db0`（不移动）  
> **本线包 / API：** `2.3.0` · `v2.3.0`（**封版就绪；`v2.3.0` tag 待用户授权**）  
> **验收：** `accept_v23.py`；`accept_v23_real.py` 默认 SKIP；`accept_v22.py` Diff 回归  
> **配套文档：** [`版本清单.md`](./版本清单.md) · [`dfine-cuda-runthrough.md`](./dfine-cuda-runthrough.md)

---

## 一、当前总体结论

Scientist Lab **v2.3** 已完成 **CUDA + Vendor DFINE** 离线就绪与门禁实跑契约（含 v2.2 受限 Diff 工作台），版本号已对齐 **2.3.0**，离线验收就绪；**尚未打 `v2.3.0` tag**。

主链覆盖（在 v2.2 之上新增）：

```text
Vendor pin / stage / adapter
→ CUDA doctor（含 GPU 深度）
→ Fast Eval / Formal triad（默认 dry-run）
→ Vendor Evidence（非 stand-in）
→ Claim Gate（path-open ≠ 优越性）
→ accept_v23_real 门禁 pipeline（默认 SKIP）
```

**不应描述为：** CUDA GPU 上 Vendor Fast Eval 已实跑通过；formal DFINE 优越性 / SOTA 成立；模型可自动改主树并发布。

---

## 二、版本进度总览

| 版本 | 状态 |
|------|------|
| v0.1～v1.9 | 已封版 |
| **v2.0 Workbench** | **已封版 `v2.0.0`** |
| **v2.1 Real LLM 闭环** | **已封版 `v2.1.0`** |
| **v2.2 受限 Diff** | **工程完成（并入 `2.3.0`；未单独打 `v2.2.0` tag）** |
| **v2.3 CUDA+DFINE** | **封版就绪 `2.3.0`（tag 待授权）** |

---

## 三～九、架构与 v2.1 结论

（与 `v2.1.0` 封版文档一致，此处不重复。）

**v2.1 对外须保留：** 工程 + 离线 Replay 已完成；`accept_v21_real` SKIP → 在线两轮证据待补跑。

---

## 十、v2.2 / v2.3 子版本

- v2.2.1–2.2.10：受限 Diff 全链路（见第十二步方案）。
- v2.3.1–2.3.8：**全部完成**（见第二十三步方案）；版本号 `2.3.0`。

规格：`docs/plans/第二十三步制作方案.md`。

---

## 十一、最终判断

```text
v2.0 = 完整 AI Scientist 工作台（已封版）
v2.1 = 真实 LLM 多轮闭环工程 + 离线 Replay（已封版；在线实跑证据待补）
v2.2 = 真实 Provider 受限 Diff（工程完成；在线 Diff 证据待补；并入 2.3.0）
v2.3 = CUDA + Vendor DFINE（封版就绪；GPU 实跑与 formal 优越性尚未证明）
```
