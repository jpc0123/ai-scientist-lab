# Scientist Lab 当前进度总结与功能分析

> **核验 / 封版日期：** 2026-07-24  
> **正式 tag：** `v2.1.0`（基线 `v2.0.0`）  
> **包版本：** `2.1.0` · **API：** `v2.1.0`  
> **验收：** `accept_v21.py` 17/17；`accept_v21_real.py` 默认 SKIP；`accept_v20.py` 30/30  
> **配套文档：** [`版本清单.md`](./版本清单.md)

---

## 一、当前总体结论

Scientist Lab **v2.1.0** 已正式封版：在 v2.0 工作台之上，支持真实模型参与的两轮科研反馈闭环（默认 Mock、显式门禁、禁止静默回退、人工审批不变）。

主链覆盖：

```text
ResearchProject / 向导
→ 数据集与环境 → 协议 → Baseline / 多 Seed
→ 聚合比较 → Evidence / Claim
→ Planner / Critic → 人工审批 → 有限实验树
→ 报告 / Audit → 真实 LLM（可选）+ 质量治理
→ 受限补丁 → 受控 Merge / Rollback / RC
→ Web 工作台 / Doctor / Recovery / 安装启动
```

**准确定位：**

> Scientist Lab v2.0 是一个本地优先、人工审批驱动、可复现、可恢复、可审计的 AI Scientist 工作台，支持实验协议、模型执行、多轮实验树、证据与结论治理、真实 LLM Provider、报告审计、受限源码补丁以及 Web 工作台。

**不应描述为：** 完全自主 AI 科学家；已完成真实 RGB-T 科研结论；模型可自动修改代码并发布；系统可不经人工干预持续研究。

工程产品已完整。真正剩余的核心缺口是：

> 真实语言模型是否能够在实验反馈中持续调整研究计划（→ **v2.1**）。

---

## 二、版本进度总览

| 版本 | 状态 |
|------|------|
| v0.1～v1.6 | 已封版（含已知延期） |
| v1.7 Web Console | 已封版 |
| v1.8 Workspace / Release | 已封版 |
| v1.9 Merge / Rollback / RC | 已封版 |
| **v2.0 Workbench** | **已封版 `v2.0.0`** |

v0.8 **仍延期：** CUDA + Vendor DFINE 禁 fallback 远程强验收。  
v1.6.7 **仍延期：** 真实 Provider 生成 Diff。

---

## 三～八、架构与能力

（与封版前分析一致，此处只列结论。）

- **对象层 / 执行层 / 证据层 / 规划层 / 报告层 / 补丁合并层 / Web 产品层**均已打通。  
- 安全边界未放松。  
- 科研结果成熟度仍偏探索性；工程成熟度高。

完整流程可做到：建项目 → 跑实验 → 规划审批 → 推进树 → 报告审计 → 补丁/合并 → Doctor/恢复。

---

## 九、阶段描述

> 已完成受控、可审计、人工审批驱动的 AI Scientist 工作流框架，并以 **v2.0.0** 形式提供统一工作台。下一阶段 **v2.1** 专注真实 LLM 多轮反馈闭环，而不是继续堆 Dashboard / CRUD。

---

## 十、下一步（v2.1）

分支：`feat/v2.1-real-llm-closed-loop`（自 `v2.0.0`）。

| 子版本 | 状态 |
|--------|------|
| **v2.1.1** 会话模型与状态机 | **已完成** |
| **v2.1.2** PlanningContext / RoundFeedbackSummary | **已完成**（第二轮无反馈则阻断） |
| **v2.1.3** real-only Planner/Critic 强制模式 | **已完成**（禁 mock/replay fallback；Profile 资格；MockTransport 离线测） |
| **v2.1.4** Round 1 审批 + Digits 执行 | **已完成**（`real-loop-approve/execute`；禁 `run_mock_experiment.py`） |
| **v2.1.5** Evidence/Claim 回填 + Round 2 上下文 | **已完成**（`record-execution-feedback` / `next-round`） |
| **v2.1.6** FeedbackUseVerifier + 方案变化 | **已完成**（六维确定性校验；重复候选阻断） |
| **v2.1.7** Replay Bundle + 离线验收 | **已完成**（`real-loop-export`；`accept_v21.py` 17/17） |
| **v2.1.8** Web Console 闭环页面 | **已完成**（`/real-loops` + REAL/MOCK/REPLAY 徽章） |
| **v2.1.9** 真实验收报告 | **已完成**（`accept_v21_real.py`；默认 SKIP / 零网络；报告 `v21_real_closed_loop_report.json`） |
| **v2.1.10** 封版 | **已完成**（正式 tag **`v2.1.0`**） |

规格：`docs/plans/第二十一步制作方案.md`。

---

## 十一、最终判断

```text
v2.0 = 完整 AI Scientist 工作台（已封版）
v2.1 = 真实模型参与实验反馈迭代的受控 AI Scientist（已封版）
```
