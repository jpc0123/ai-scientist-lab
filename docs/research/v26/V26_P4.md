# V26.7 P4 — RT-DETR Cross-model Transfer Probe

日期：2026-08-21  
战役：v2.6 / P4  
**角色：Cross-model Transfer Probe。不是 Cross-model Generalization Validation。**  
Protocol：`research_protocol_rgbt_rtdetr_transfer_v26`  
Adapter：`rtdetr`（HOW，不是第五个 Agent）  
ClaimGate：**C0 / BLOCKED**。Verdict：**INCONCLUSIVE**。不是 G2。不是 G3。未 BAN F3。未 commit / push。

STOP 裁决：[`DECISION_STOP_P4.md`](./DECISION_STOP_P4.md)。C0 分析：[`V26_P4_C0_ANALYSIS.md`](./V26_P4_C0_ANALYSIS.md)。

## 决策

用户在 R5 闭合后说「继续」= Human Gate **GO P4**。裁决：[`DECISION_P4_GO.md`](./DECISION_P4_GO.md)。

不跑 D-FINE R6。不把 D-FINE `max_rounds=6` 再抬高。

## 注册了什么

| 项 | ID |
|----|----|
| Adapter | `rtdetr` / `RTDETRAdapter` |
| Baseline key | `rtdetr_s` |
| Protocol | `research_protocol_rgbt_rtdetr_transfer_v26` |
| Fingerprint id | `FP-RGBT-RTDETR-V26-TRANSFER-LOWLIGHT` |
| Dataset fingerprint（冻） | `787c65bda4eb31a700e67fba7804f1e7e17079574d24c02399f74bb722d27ea6` |
| HOW allowlist | F0 / F1 / F3 / N0 / N1（N1 在本 Adapter 上拒绝；不注册 F2/T1/T2/A4） |
| `max_rounds` | **2**（R0 F1 + R1 F3） |
| `max_claim_strength` | **C0** |

检测器身份：HGNetv2-B0 + HybridEncoder + **RTDETRTransformer**（lyuwenyu zoo）。  
不是把 DFINETransformer 改名。Fusion HOW（early_concat / gated_multiscale）由 Adapter 翻译。

## 数据合同（未改）

- `dataset_id=rgbt_tiny_v1`
- `slice_id=low_light_subset_v1`
- Primary = `APS_lowlight`（切片 val，pycocotools）
- 与 D-FINE R0/R5 **方向对照**，不宣称跨模型 matched C1

## Packs

| Pack | HOW | seed | APS_lowlight | 角色 |
|------|-----|------|--------------|------|
| `outputs/v26_p4_r0` | F1 `early_concat` | 42 | **0.013241256515861267** | RT-DETR 基线（已完成） |
| `outputs/v26_p4_r1` | F3 `gated_multiscale` | 42 | **0.014949471669213135** | 策略迁移（已完成） |

## GPU 事实

- 权重存在：`third_party/DFINE/weight/hgnetv2/PPHGNetV2_B0_stage1.pth`。不是缺权重 STOP。
- 第一次 GPU（`exec_eeddf3700e3f`）：`RTDETRCriterion.forward()` 不接 D-FINE `epoch=`。已补 `**kwargs` + unwrap `HungarianMatcher['indices']`。
- 第二次 GPU：训练开始后 CDN denoising 损失 NaN（AMP 开/关都复现）。已关 RT-DETR AMP，并把该 160 预算下 `num_denoising=0`。
- 第三次 GPU（`exec_5cdd236bde02`，`return_code=0`，约 15 min）：HOW=F1 seed=42，`APS_lowlight=0.013241256515861267`（pycocotools，切片 val 100 图）。`metrics_forged=false`。Rubric **REPLICATE**。ClaimGate **C0 observational**。**不是 G2。**
- 第四次 GPU / F3 迁移（`exec_6b36673902b9`，`return_code=0`，约 20 min）：HOW=F3 `GatedMultiscaleFusion` seed=42，`APS_lowlight=0.014949471669213135`。相对 RT-DETR F1 **+0.001708**。Rubric **KEEP**。ClaimGate **BLOCKED**（拟 C1 超过协议 max C0）。`scientific_outcome=INCONCLUSIVE`。**KEEP ≠ Claim。不是 G3。不是 transfer success。**
- 文献 live `semantic_scholar`（F1 `litq_2b6986bb2ab0`；F3 `litq_a656103ab57c`）不得进 ClaimGate。

方向对照（C0，不可对外宣称跨模型 C1 / G2 / G3）：

| 模型 | HOW | APS_lowlight |
|------|-----|--------------|
| D-FINE R0 | F1 | 0.0045926865160844455 |
| D-FINE R1 | F3 seed42 | 0.02135704762627717 |
| RT-DETR P4 R0 | F1 | 0.013241256515861267 |
| RT-DETR P4 R1 | F3 seed42 | 0.014949471669213135 |

C0 分析（无新 GPU）：[`V26_P4_C0_ANALYSIS.md`](./V26_P4_C0_ANALYSIS.md) · [`DECISION_P4_C0_ANALYSIS.md`](./DECISION_P4_C0_ANALYSIS.md) · [`DECISION_STOP_P4.md`](./DECISION_STOP_P4.md) · `outputs/v26_p4_analysis/summary.json` · `outputs/v26_p4_analysis/strategy_card_gated_multiscale.json`。F3−F1 主指标微涨但 AP50_lowlight / 全集 mAP50 下降；跨模型 Δ 约为 D-FINE 同 seed 的 0.102。**INCONCLUSIVE probe。不是 transfer success。不是 F3 无效。未 BAN gated_multiscale。**
## CLI

## CLI

```text
scientist-lab v26-p4-run --stage r0 --execute --confirm-human-gate --require-live-ready --live
scientist-lab v26-p4-run --stage transfer --execute --confirm-human-gate --require-live-ready --live
```

Live 文献：Semantic Scholar。LiteratureEvidence **不得**进 ClaimGate。

## 可见性

- http://127.0.0.1:5174/loop/v26_p4_r0
- http://127.0.0.1:5174/loop/v26_p4_r1
- http://127.0.0.1:5174/training
