# Scientist Lab 版本清单

> **核验日期：** 2026-07-25  
> **正式发行（上一冻结）：** **`v2.1.0`** @ `bb47db0`（不移动 Tag）  
> **本线正式发行：** **`v2.3.0`** @ `0befdd7`（包版本 `2.3.0`，API `API_VERSION=v2.3.0`；含 v2.2 Diff 工作台）  
> **Tag 状态：** **已打 `v2.3.0`**（commit `0befdd77c45972507e3f9191ccf8dd656681a229`）  
> **说明：** `v2.2.0` 未单独打 tag；Diff 能力并入 `v2.3.0` 发行线。

---

## 1. 当前快照

| 项 | 状态 |
|----|------|
| 正式 tag（已冻结） | **`v2.1.0`** @ **`bb47db0`**；**`v2.3.0`** @ **`0befdd7`** |
| 当前分支 | `feat/v2.2-real-provider-restricted-diff` |
| 包 / CLI | **`2.3.0`**（`scientist-lab --version`） |
| API health / Doctor | **`v2.3.0`** |
| Web `package.json` | **`2.3.0`** |
| 离线验收 | `accept_v23.py`（主链）；`accept_v22.py`（Diff 回归） |
| 真实验收 | `accept_v23_real.py` / `accept_v22_real.py`（默认 SKIP） |
| 当前分支 HEAD | **`0befdd7`**（`v2.3.0` 封版提交） |

---

## 2. 版本号对照（v2.3.0 封版目标）

| 位置 | 值 |
|------|-----|
| `pyproject.toml` | `2.3.0` |
| `src/scientist_lab/__init__.py` → `__version__` | `2.3.0` |
| `src/scientist_lab/__init__.py` → `API_VERSION` | `v2.3.0` |
| CLI `--version` | `scientist-lab 2.3.0` |
| FastAPI `app.version` | `2.3.0` |
| `/api/v1/health` → `version` | `v2.3.0` |
| Web `package.json` / 页面 eyebrow | `2.3.0` |
| Git tag（已打） | **`v2.3.0`** @ **`0befdd7`** |
| `CHANGELOG.md` | `[2.3.0]` |

---

## 3. Tag 历史（摘要）

| Tag | Commit（封版时） | 主题 | 验收 |
|-----|------------------|------|------|
| … | … | … | … |
| **`v2.1.0`** | **`bb47db0`** | **真实 LLM 多轮闭环** | **`accept_v21`；`accept_v21_real` SKIP** |
| **`v2.2.0`** | *未打（并入 2.3）* | **真实 Provider 受限 Diff** | **`accept_v22`；`accept_v22_real` 默认 SKIP** |
| **`v2.3.0`** | **`0befdd7`** | **CUDA + Vendor DFINE** | **`accept_v23`；`accept_v23_real` 默认 SKIP** |

---

## 4. 对外结论（须保留）

### v2.3

```text
v2.3 已完成 Vendor DFINE 离线就绪、CUDA doctor / dry-run 编排、Claim Gate 与门禁实跑契约；
由于 accept_v23_real.py 默认 SKIP，CUDA GPU 上 Vendor Fast Eval 证据仍待补跑。
不宜写成「CUDA + DFINE 正式科研结论已经实跑通过」，更不宜写成 formal 优越性成立。
```

### v2.2

```text
v2.2 已完成真实 Provider 受限 Unified Diff 的工程闭环与离线 Replay / Web 验收；
由于 accept_v22_real.py 默认 SKIP，真实云模型在线 Diff 证据仍待补跑。
```

---

## 5. 阶段定位

| 状态 | 能力 | 版本 |
|------|------|------|
| **已封版** | 真实 LLM 多轮闭环（工程 + 离线 Replay） | **v2.1.0** |
| **已封版** | CUDA + Vendor DFINE（含 v2.2 Diff） | **v2.3.0** @ **0befdd7** |
| 待补跑 | `accept_v21_real` / `accept_v22_real` / `accept_v23_real` | 门禁在线证据 |

### v2.3.1–v2.3.8 已交付

```text
vendor_audit / cuda_doctor(+GPU深度) / orchestrator / evidence / formal_triad
formal_dfine_gate / dfine_real_acceptance
演示: scripts/demo_dfine_cuda_offline.py
指南: docs/dfine-cuda-runthrough.md
CLI: dfine-cuda-doctor / dfine-cuda-fast-eval / dfine-cuda-record-evidence
     / dfine-cuda-formal-triad / dfine-formal-path-gate / dfine-real-acceptance
版本号对齐 2.3.0；docs/acceptance/v2.3/
accept_v23.py（离线）/ accept_v23_real.py（默认 SKIP）
规格: docs/plans/第二十三步制作方案.md
```

### 验收命令

```powershell
python .\scripts\accept_v23.py
python .\scripts\accept_v23_real.py
python .\scripts\accept_v22.py
scientist-lab --version
```

---

## 6. 验收脚本

| 脚本 | 版本 |
|------|------|
| `scripts/accept_v23.py` | v2.3 离线主链 |
| `scripts/accept_v23_real.py` | v2.3 真实（门禁；默认 SKIP） |
| `scripts/accept_v22.py` | v2.2 Diff 回归 |
| `scripts/accept_v22_real.py` | v2.2 真实（门禁；默认 SKIP） |
| `scripts/accept_v21.py` | v2.1 离线 |
| `scripts/accept_v20.py` | v2.0 |

---

## 7. 封版操作（已完成）

```text
1. accept_v23.py 全绿（15/15）
2. git commit 0befdd7 — feat: complete Scientist Lab v2.3 CUDA Vendor DFINE seal
3. git tag v2.3.0（不移动既有 v2.1.0）
4. 可选：git bundle → backups/；push 需另授权
```
