# scientist-lab 使用说明（Windows）

适合第一次本地启动。按顺序做即可。

---

## 0. 先分清：CMD 还是 PowerShell

你当前窗口提示符类似：

```text
D:\AI Scientist_tiao\scientist-lab>
```

这是 **命令提示符（CMD）**，不是 PowerShell。

| 终端 | 激活虚拟环境应使用 |
|------|-------------------|
| CMD（`cmd.exe`） | `.\.venv\Scripts\activate.bat` |
| PowerShell | `.\.venv\Scripts\Activate.ps1` |

常见错误：

```text
cd /d "...\.venv\Scripts\Activate.ps1"
```

这会报「目录名称无效」，因为 `Activate.ps1` 是**脚本文件**，不是文件夹，不能 `cd` 进去。

另一个错误：在 CMD 里直接跑 `Activate.ps1`，环境经常**看起来执行了，其实没激活成功**，后面就会出现：

```text
'scientist-lab' 不是内部或外部命令
```

---

## 1. 前置条件

1. 已安装 Python 3.11+（你这边是 3.12 即可）
2. **Docker Desktop 已打开**（托盘图标在运行）
3. 工程目录存在：`D:\AI Scientist_tiao\scientist-lab`

---

## 2. 推荐启动方式 A：用 CMD（最简单）

打开「命令提示符」，完整复制执行：

```bat
cd /d "D:\AI Scientist_tiao\scientist-lab"

.\.venv\Scripts\activate.bat

docker build -t scientist-experiment:v1 docker

scientist-lab doctor

scientist-lab run examples\smoke_test_contract.json

scientist-lab list-executions
```

成功时：

- 激活后，提示符前面会出现 `(.venv)`
- `doctor` 会输出 `Docker OK`
- `run` 会打印一段 JSON，`status` 为 `completed`

### 如果仍然提示找不到 `scientist-lab`

说明虚拟环境没激活成功，或以前没用 `pip install -e .` 装过。改用**完整路径**（不依赖 PATH）：

```bat
cd /d "D:\AI Scientist_tiao\scientist-lab"

.\.venv\Scripts\python.exe -m pip install -e ".[dev]"

.\.venv\Scripts\scientist-lab.exe doctor

.\.venv\Scripts\scientist-lab.exe run examples\smoke_test_contract.json

.\.venv\Scripts\scientist-lab.exe list-executions
```

查看某次执行（把 ID 换成你自己的）：

```bat
.\.venv\Scripts\scientist-lab.exe show-execution exec_xxxxxxxx
```

---

## 3. 推荐启动方式 B：用 PowerShell

1. 开始菜单搜索 **Windows PowerShell** 或 **终端**
2. 执行：

```powershell
cd "D:\AI Scientist_tiao\scientist-lab"

Set-ExecutionPolicy -Scope Process -ExecutionPolicy Bypass

.\.venv\Scripts\Activate.ps1

docker build -t scientist-experiment:v1 docker

scientist-lab doctor
scientist-lab run examples/smoke_test_contract.json
scientist-lab list-executions
```

若提示「无法加载，因为在此系统上禁止运行脚本」，上面的 `Set-ExecutionPolicy` 只对当前窗口生效，一般即可。

同样可用完整路径：

```powershell
.\.venv\Scripts\scientist-lab.exe doctor
.\.venv\Scripts\scientist-lab.exe run examples\smoke_test_contract.json
```

---

## 4. 如果你还没有虚拟环境

只在第一次或删掉 `.venv` 后需要：

### CMD

```bat
cd /d "D:\AI Scientist_tiao\scientist-lab"
python -m venv .venv
.\.venv\Scripts\activate.bat
python -m pip install --upgrade pip
pip install -e ".[dev]"
```

### PowerShell

```powershell
cd "D:\AI Scientist_tiao\scientist-lab"
python -m venv .venv
.\.venv\Scripts\Activate.ps1
python -m pip install --upgrade pip
pip install -e ".[dev]"
```

---

## 5. 命令说明（都要加前缀 `scientist-lab`）

不要单独输入 `doctor` 或 `run ...`。

| 正确写法 | 错误写法 |
|----------|----------|
| `scientist-lab doctor` | `doctor` |
| `scientist-lab run examples\smoke_test_contract.json` | `run examples/...` |
| `scientist-lab list-executions` | `list-executions` |
| `scientist-lab show-execution <id>` | 只输入 id |

含义：

| 命令 | 作用 |
|------|------|
| `scientist-lab doctor` | 检查 Docker、数据库路径、镜像配置 |
| `scientist-lab run <契约.json>` | 跑一轮本地 Docker 实验 |
| `scientist-lab list-executions` | 列出历史执行 |
| `scientist-lab show-execution <execution_id>` | 查看一次执行和产物 |

---

## 6. 你刚才那次操作的对照

| 你执行的命令 | 结果原因 | 正确做法 |
|--------------|----------|----------|
| `cd ...\Activate.ps1` | 那是文件，不是目录 | 不要 `cd` 到脚本 |
| `.\.venv\Scripts\Activate.ps1`（在 CMD） | 容易激活失败 | CMD 用 `activate.bat` |
| `docker build ...` | 成功 | 保持即可 |
| `scientist-lab doctor` | 未进 PATH（venv 未激活） | 先激活，或用 `.\.venv\Scripts\scientist-lab.exe` |
| `doctor` / `run ...` | 缺少程序名 | 写成 `scientist-lab doctor` 等 |

---

## 7. 最短可用流程（CMD，复制即用）

```bat
cd /d "D:\AI Scientist_tiao\scientist-lab"
.\.venv\Scripts\activate.bat
.\.venv\Scripts\scientist-lab.exe doctor
.\.venv\Scripts\scientist-lab.exe run examples\smoke_test_contract.json
.\.venv\Scripts\scientist-lab.exe list-executions
```

看到 `Docker OK` 和 `status: completed`，就说明已经启动成功。

---

## 8. 启动网页实验控制台（第二步）

先确保 Docker 已开、镜像已构建，然后：

```bat
cd /d "D:\AI Scientist_tiao\scientist-lab"
.\.venv\Scripts\activate.bat
.\.venv\Scripts\python.exe -m pip install -e ".[dev]"
scientist-lab serve
```

浏览器打开：

```text
http://127.0.0.1:8787
```

页面功能：

1. **项目**：查看项目和最近执行  
2. **新建实验**：表单或 JSON 提交契约  
3. **执行详情**：状态、metrics、日志、取消  
4. **对比**：比较两次 execution  

停止服务：在运行 `serve` 的窗口按 `Ctrl + C`。

---

## 9. 常见问题

### Docker 相关报错「无法连接 Docker」

先打开 **Docker Desktop**，等状态变为 Running，再执行 `scientist-lab doctor`。

### 提示镜像不存在

```bat
docker build -t scientist-experiment:v1 docker
docker build -t scientist-experiment:v2 docker
```

mock / 失败路径用 **v1**；Digits 真实训练用 **v2**（契约 `environment_key`: `digits-mlp-v1`）。

### 提示 `pip` / `python` 找不到

确认本机 Python 在 PATH；或用：

```bat
py -3 -m venv .venv
```

### 想退出虚拟环境

```bat
deactivate
```

---

## 10. 跑通后产物在哪

- 数据库：`scientist_lab.db`
- 实验输出：`outputs\project_001\exec_xxxxxxxx\`
  - `metrics.json`
  - `artifact_manifest.json`
  - `combined.log`
  - `contract.json` / `config.json` / `execution.json`
  - 真实实验额外：`model.joblib` / `training_curve.csv` / `confusion_matrix.csv`

---

## 11. 跑 Digits 真实实验

```bat
docker build -t scientist-experiment:v2 docker
scientist-lab run examples\digits_real_contract.json
scientist-lab run examples\digits_real_contract_02.json
scientist-lab compare-executions <id_a> <id_b>
```

验收参考：`status=completed`，`accuracy` / `macro_f1` 通常 ≥ 0.85。网页控制台「场景模板」里也有 Digits 预设。

单变量对照比较（第三步）：

```bat
scientist-lab compare-nodes node_003 node_004
```

应看到 `experiment_valid=true`、`hypothesis_status`、以及 `conclusion`。

多 seed 聚合比较（第四步）：

```bat
scientist-lab run-seeds examples\digits_real_contract.json --seeds 42,43,44,45,46
scientist-lab run-seeds examples\digits_real_contract_02.json --seeds 42,43,44,45,46
scientist-lab aggregate-node node_003
scientist-lab compare-node-groups node_003 node_004
```

聚合文件：`outputs\project_001\node_003\aggregate_metrics.json`。

规则反馈与下一轮草案（第五步）：

```bat
scientist-lab analyze-feedback node_003 node_004
scientist-lab show-feedback node_004
scientist-lab propose-next node_003 node_004
```

查看生成的草案后再人工 `run-seeds`（不会自动提交）。

效率折中 / 实用等价（004 vs 005）：

```bat
scientist-lab compare-node-groups node_004 node_005
scientist-lab analyze-feedback node_004 node_005
scientist-lab record-decision --selected node_005 --alternatives node_003,node_004 --decision-type efficiency_tradeoff --reason "Comparable mean accuracy to node_004 with lower runtime; less stable." --baseline-node-id node_004 --candidate-node-id node_005
```
